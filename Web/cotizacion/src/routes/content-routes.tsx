import React from 'react'
import { Route, Routes, Outlet, useLocation, Navigate } from "react-router-dom";

export const ContentRoutes = () => {
  return (
    <>      <Routes>        <Route path="coti">
     </Route>  </Routes>
    </>
  )
}
